{
  "name": "lran-config",
  "version": "0.1.0",
  "description": "LRAN runtime configuration: the hand-written parameter table every other copy is derived from (D44), and the store that applies a CONFIG set and answers a readback. Arduino-free; persistence is injected. Binding specification: LRAN-Protocol-Specification v0.12 (ver = 2).",
  "keywords": ["lran", "configuration", "home-assistant"],
  "license": "MIT",
  "frameworks": "*",
  "platforms": "*",
  "headers": "lran/config/table.h",
  "dependencies": [{ "name": "lran-protocol" }],
  "build": {
    "srcDir": "src",
    "includeDir": "include",
    "flags": ["-std=gnu++17"]
  }
}
