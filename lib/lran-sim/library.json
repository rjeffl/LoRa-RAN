{
  "name": "lran-sim",
  "version": "0.1.0",
  "description": "LRAN malformed-frame primitives for the simnode fault catalogue - a patch applied after the real encoder, never a second serializer (Bridge Impl Plan 10.5.2, 10.6). Arduino-free. Binding specification: LRAN-Protocol-Specification v0.11 (ver = 2).",
  "keywords": ["lora", "lran", "fault-injection"],
  "license": "MIT",
  "frameworks": "*",
  "platforms": "*",
  "headers": "lran/sim/frame_patch.h",
  "dependencies": [{ "name": "lran-protocol" }],
  "build": {
    "srcDir": "src",
    "includeDir": "include",
    "flags": ["-std=gnu++17"]
  }
}
